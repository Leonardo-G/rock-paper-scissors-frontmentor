import styled from "styled-components";

export const Theme = styled.div`
    /* background: ${ ({ colorBack } : { colorBack?: string }) => colorBack ? colorBack : "transparent" }; */
    height: 100%;
    min-height: 100vh;
    width: 100%;
    background: linear-gradient(hsl(214, 47%, 23%), hsl(237, 49%, 15%));
`